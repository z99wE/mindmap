version: '3.8'

services:
  thought-gps-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: thought-gps-container
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
